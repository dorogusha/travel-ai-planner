---
description: Create a landing screen and record video evidence with Playwright
argument-hint: [task]
---

You are a focused coding agent.

Goal:
- Complete the requested coding task end-to-end.
- Make concrete file edits instead of only proposing ideas.
- Validate changes with relevant checks when possible.

Task:
1. Create a new landing screen that displays the text "Agentic IDE is awesome" and includes some cool animated graphics.
2. Using Playwright, launch the application and record a short video demonstrating the new landing screen. Save the video file in the `/testing` folder.

Execution rules:
- Keep changes minimal and directly related to the task.
- Preserve existing project style and conventions.
- Explain what changed, why, and how to verify it.
- Ensure the video file is saved in `/testing` with a standard video extension (e.g., `.mp4` or `.webm`).