Specification: Search Bar Component

Task Description

Create a Vue component for the current project that:

- Renders a search input at the top of the map, similar to Google Maps.

- Uses Google Places API for autocomplete and location search.

- On selecting a location, centers the map on that point and opens the side panel with location info.

AI Prompt

Create a Vue 3 component for the current project that renders a search input at the top of the map, using Google Places API for autocomplete. When a location is selected, center the map on that point and open the side panel with location info.